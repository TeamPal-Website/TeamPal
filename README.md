# Team Pal

Платформа для подбора участников и формирования команд под учебные, pet- и стартап-проекты.

Текущий фокус репозитория: `Спринт 2. Ядро и аутентификация`.

## О проекте

Проект выполняется в рамках кейса:
`Создание цифровой платформы для подбора участников и формирования команд`.

Цель кейса - разработать MVP веб-сервиса, который позволит:

- регистрироваться, авторизоваться и управлять профилем;
- создавать карточки проектов с описанием идеи, стека и ролей;
- искать пользователей и проекты по ключевым словам и навыкам.

В текущем спринте основная задача команды - поднять базовую структуру приложения и реализовать `AuthService` вместе со сквозным auth flow через backend, frontend и тесты.

## Текущий спринт

### Спринт 2. Ядро и Аутентификация

Спринт соответствует требованиям второго контрольного показа из ТЗ.

Задачи спринта:

- поднять базовую структуру проекта `frontend + backend`;
- реализовать модель пользователя и хранение данных;
- сделать регистрацию, вход и выход;
- подготовить минимально работающие страницы приложения после авторизации;
- собрать техническую документацию спринта.

Ожидаемый результат показа:

- пользователь может зарегистрироваться;
- пользователь может войти в аккаунт;
- пользователь может выйти из аккаунта;
- backend возвращает данные текущего пользователя;
- базовые страницы приложения доступны и связаны с API.

## Roadmap По Спринтам

- `Спринт 1 (Недели 1-3)` - планирование, архитектура, схема БД, API-контракт, wireframes, декомпозиция задач.
- `Спринт 2 (Недели 4-6)` - ядро приложения и аутентификация.
- `Спринт 3 (Недели 7-9)` - профили пользователей и карточки проектов.
- `Спринт 4 (Недели 10-12)` - поиск, заявки на участие, базовый MVP-цикл.
- `Спринт 5 (Недели 13-15)` - тестирование, стабилизация, рефакторинг, деплой и подготовка к защите.

## Что Уже Реализовано

- backend на `FastAPI`;
- модели пользователей и миграция таблицы `users`;
- `AuthService` для работы с паролями и JWT;
- регистрация пользователя;
- вход пользователя;
- выход пользователя;
- получение данных текущего пользователя по cookie-токену;
- базовая блокировка и разблокировка пользователя;
- статическая раздача frontend-страниц;
- unit и integration тесты для auth-логики, схем и репозиториев.

## AuthService

`AuthService` - центральный сервис текущего спринта. Он отвечает за:

- хэширование паролей;
- проверку паролей при логине;
- создание JWT access token;
- декодирование и валидацию токена;
- обработку ошибок просроченного или невалидного токена.

На текущем этапе токен кладется в cookie `access_token`, а доступ к защищенным ручкам идет через dependency в backend.

## Архитектура Backend

В проекте используется слоистая структура:

- `api` - HTTP-роуты и зависимости FastAPI;
- `services` - бизнес-логика, связанная с аутентификацией;
- `repositories` - работа с базой данных;
- `models` - ORM-модели SQLAlchemy;
- `schemas` - Pydantic-схемы для входных и выходных данных;
- `utils` - служебные компоненты, например `DBManager`;
- `migrations` - Alembic-миграции.

Основной auth flow:

1. Пользователь отправляет `email` и `password`.
2. Backend валидирует входные данные через Pydantic-схемы.
3. При регистрации пароль хэшируется через `AuthService`.
4. При логине пароль сверяется с хэшем, после чего создается JWT.
5. JWT сохраняется в cookie и используется для доступа к защищенным маршрутам.

## Стек Технологий

### Backend

- `Python 3`
- `FastAPI`
- `SQLAlchemy 2`
- `Alembic`
- `Pydantic v2`
- `PyJWT`
- `pwdlib`
- `PostgreSQL`

### Frontend

- `HTML`
- `CSS`
- `Vanilla JavaScript`

### Тестирование

- `pytest`
- `pytest-asyncio`
- `httpx`
- `SQLite in-memory` для тестовых сценариев

## Структура Репозитория

```text
Team_Pal/
├── backend/
│   ├── alembic.ini
│   ├── requirements.txt
│   └── src/
│       ├── api/
│       ├── migrations/
│       ├── models/
│       ├── repositories/
│       ├── schemas/
│       ├── services/
│       ├── utils/
│       ├── config.py
│       ├── database.py
│       └── main.py
├── frontend/
│   ├── authorization_page.html
│   ├── registration_page.html
│   ├── account.html
│   ├── main_page.html
│   └── config.js
└── tests/
    ├── integration/
    └── unit/
```

## API Текущего Спринта

### Auth

- `POST /auth/register` - регистрация пользователя
- `POST /auth/login` - вход пользователя
- `GET /auth/me` - получение текущего пользователя
- `POST /auth/logout` - выход пользователя

### Admin Utilities

- `POST /admins/block` - блокировка пользователя
- `POST /admins/unblock` - разблокировка пользователя

### Документация FastAPI

- `GET /docs`
- `GET /redoc`

## Пример Переменных Окружения

Файл: `backend/.env`

```env
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=team_pal

JWT_SECRET_KEY=super-secret-key
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5500
```

## Локальный Запуск

Все команды ниже предполагают запуск из корня репозитория `Team_Pal`, если не указано иное.

### 1. Клонирование проекта

```bash
git clone <repo_url>
cd Team_Pal
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей backend

```bash
pip install -r backend/requirements.txt
```

### 4. Настройка `.env`

Создать и заполнить файл `backend/.env` по примеру выше.

### 5. Применение миграций

```bash
alembic -c backend/alembic.ini upgrade head
```

### 6. Запуск backend

```bash
uvicorn --app-dir backend src.main:app --reload
```

Backend будет доступен по адресу:

```text
http://127.0.0.1:8000
```

### 7. Запуск frontend

На текущем этапе frontend раздается самим backend через `StaticFiles`, поэтому после запуска сервера можно открывать:

- `http://127.0.0.1:8000/`
- `http://127.0.0.1:8000/authorization_page.html`
- `http://127.0.0.1:8000/registration_page.html`
- `http://127.0.0.1:8000/account.html`

## Тестирование

Запуск всех тестов:

```bash
pytest
```

Запуск только auth-тестов:

```bash
pytest tests/unit/test_auth_service.py tests/integration/test_auth_api.py
```

В репозитории уже есть:

- unit-тесты для `AuthService`;
- unit-тесты для схем;
- integration-тесты для auth API;
- integration-тесты для репозиториев;
- integration-тесты для админских ручек.

## Критерии Готовности К Показу 2

Спринт можно считать закрытым, если:

- backend поднимается локально без ручных правок кода;
- пользователь успешно регистрируется;
- пользователь успешно логинится;
- cookie с `access_token` устанавливается после входа;
- `GET /auth/me` возвращает данные текущего пользователя;
- `POST /auth/logout` завершает текущую сессию;
- базовые страницы frontend доступны после запуска приложения;
- в репозитории есть актуальная техническая документация.

## Ограничения Текущего Спринта

В этот спринт не входят:

- полноценное редактирование профиля;
- навыки пользователя;
- создание и редактирование проектов;
- поиск пользователей и проектов;
- заявки на участие;
- финальный деплой и polish UI/UX.

## Следующий Этап

Следующий логичный шаг - `Спринт 3`, в котором нужно реализовать:

- профиль пользователя;
- описание и редактирование профиля;
- навыки;
- создание и редактирование карточек проектов;
- сквозной сценарий работы через UI и API.

## Статус

Проект находится на этапе `Спринт 2 / Показ 2` и концентрируется на ядре backend и реализации аутентификации через `AuthService`.
